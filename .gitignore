.env
.env.*
!.env.example

# Audit reports are point-in-time snapshots, not maintained docs. Never commit.
# Each report carries its own staleness notice + generation commit hash in its header.
docs/audits/
